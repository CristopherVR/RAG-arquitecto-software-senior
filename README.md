# RAG-arquitecto-software-senior
Sistema de Inteligencia y Trazabilidad de Software

instalar dependencias con requirements.txt 
pip install -r requirements.txt

primero corre python -m app.main para recopilar la data,
luego corre python -m app.test_search para reconstruir la data y poder preguntarle